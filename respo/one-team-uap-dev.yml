spring:
  application:
    name: one-team-uap
  zipkin:
    base-url: http://${eureka.instance.hostname}:9411
server:
  port: 2335

eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:2333/eureka/
  instance:
    hostname: localhost

# 开启熔断器
feign:
  hystrix:
    enabled: true